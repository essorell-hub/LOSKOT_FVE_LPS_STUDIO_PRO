# Post-Mega Playwright White Screen Audit

This document defines the future UI audit path. It does not change package.json.

## Goals

- start the app
- capture screenshot
- detect empty root
- collect console errors
- collect trace when possible
- protect Classic PRO layout

## Rule

If Playwright cannot run from the repository without package changes, the audit stays external in the Night Factory artifact folder.
